import styled from '@emotion/styled';

import { css } from '@mui/material';

import { EmailSubtitle } from './EmailSubtitle';

export const EmailSubtitleSmall = styled(EmailSubtitle)(css`
  font-size: 0.9rem;
`);
