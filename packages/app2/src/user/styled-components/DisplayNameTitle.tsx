import styled from '@emotion/styled';
import { DisplayName } from './DisplayName';
import { largeFontStyle } from '../../utils/styles/large-font';

export const DisplayNameTitle = styled(DisplayName)(largeFontStyle);
