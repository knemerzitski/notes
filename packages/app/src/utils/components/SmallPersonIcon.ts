import styled from '@emotion/styled';
import PersonIcon from '@mui/icons-material/Person';

import { smallIconStyle } from '../styles/small-icon';

export const SmallPersonIcon = styled(PersonIcon)(smallIconStyle);
