extend type Query {
  "All active CollabTexts that the user has accessed"
  allActiveCollabTexts: [CollabText!]!
}

extend type CollabText {
  "Text that is displayed to the user. Includes submitted and local changes."
  viewText: String!
}
