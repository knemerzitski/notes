import styled from '@emotion/styled';
import { Typography } from '@mui/material';
import { ellipsisStyle } from '../../utils/styles/ellipsis';

export const EmailSubtitle = styled(Typography)(ellipsisStyle);
