import { ReactNode } from 'react';
import { GraphQLService } from '../types';

export function GraphQLServiceProvider({
  service,
  children,
}: {
  service: GraphQLService;
  children: ReactNode;
}) {
  //TODO create all required context providers here
}
