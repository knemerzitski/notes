import styled from '@emotion/styled';
import PersonIcon from '@mui/icons-material/Person';

import { largeIconStyle } from '../styles/large-icon';

export const LargePersonIcon = styled(PersonIcon)(largeIconStyle);
