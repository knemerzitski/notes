/**
 * Determine same position insertion bias for both server and client.
 *
 * - `false` - Insertions arriving to server later on same position are inserted after.
 * - `true` - Insertions arriving to server later on same position are inserted before.
 */
export const INSERT_BIAS = false;
